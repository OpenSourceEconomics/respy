name: respy
channels:
  - defaults
  - numba
dependencies:
  - python=3.6
  - pandas=0.20
  - numba=0.43
  - numpy
  - scipy=0.19
  - statsmodels=0.8.0
  - pip:
    - pytest
    - pytest-cov
    - pytest-pythonpath
    - codecov
