name: respy
channels:
  - defaults
  - numba
dependencies:
  - python=3.6
  - pandas=0.24
  - numba=0.43
  - numpy
  - scipy>=0.19
  - statsmodels=0.9
  - pip:
    - pytest
    - pytest-cov
    - codecov
    - pyaml
